<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zx.share.platform.wechat.mapper.UserMapper">

    <resultMap id="dictionaryResultBean" type="com.zx.share.platform.vo.wechat.response.DictionaryResultBean">
        <result column="code" property="code" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="findType" resultMap="userResultBean">
        select code,namefrom sys_dictionary where type=#{type} and parent_id=#{parentId} and status=1
    </select>

</mapper>